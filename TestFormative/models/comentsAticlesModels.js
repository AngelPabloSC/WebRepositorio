class CommentsArticles {
  constructor(id, comment, referenceUser,
    articlereference) {
    (this.id = id),
      (this.comment = comment),
      (this.referenceUser = referenceUser),
      (this.articlereference =articlereference)
    
  }
}

export default CommentsArticles;